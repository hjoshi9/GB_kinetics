# Packages required for running GB kinetics.
#
# Version floors are set by the APIs the code actually uses, not by habit.
# Tested on numpy 2.5.2, scipy 1.18.0, POT 0.9.6.post1, matplotlib 3.11.0,
# sphinx 9.1.0, sphinx_rtd_theme 3.1.0 (Python 3.14).

# Core dependencies
numpy>=1.24,<3          # <3 guards against the next set of removals
scipy>=1.10             # linear_sum_assignment, cKDTree, griddata
POT>=0.9                # ot.emd, ot.bregman.sinkhorn_log, ot.plot

# Diagnostics and plotting dependencies
matplotlib>=3.6         # subplot_kw projection='3d' without importing mplot3d

# Documentation dependencies
sphinx>=7.0
sphinx_rtd_theme>=2.0
